import React from "react";
import PropTypes from "prop-types";

const ShowCvPopUp = ({ children }) => {
  return (
    <>
      <h1>Siema</h1>
      {children}
    </>
  );
};

export default ShowCvPopUp;
